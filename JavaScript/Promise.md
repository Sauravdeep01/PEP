call stack => sync code run hota hai.

Web API's => fetch, setTimeoutm DOM events

Micro task queue => Promise.then, Async/await

callback queue => setTimemout, setInterval

Event loop => check karta hai stack empty? then queue se kaam bhejta hai